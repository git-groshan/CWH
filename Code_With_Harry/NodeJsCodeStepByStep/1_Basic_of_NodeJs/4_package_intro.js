const color = require('colors');

console.log("Roshan in Red".red);

//NodeJs is Single Threaded like JavaScript 

/**
 Q- What if we deleted the nodemdoule folder ?
 Ans-We can install it back by npm install 
 npm install command install all the packages which are there in package.json file 

 Q- How to push nodeJs project on git?
 Ans- We generally don't need to push the nodemodule folder as they are heavy.
 To avoid pushing nodemodule folder , we made .gitignore file inside nodemodule folder 
 and there we mention that we don't need to push these files/folders
 
 */